# Core PDF and Image Processing
pdf2image>=1.16.0
Pillow>=10.0.0

# AI/ML APIs
openai>=1.0.0
google-generativeai>=0.3.0

# Environment and Configuration
python-dotenv>=1.0.0

# Database (built-in sqlite3, no package needed)
# Data Processing (built-in json, csv, no packages needed)
